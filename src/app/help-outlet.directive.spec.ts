import { HelpOutletDirective } from './help-outlet.directive';

describe('HelpOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new HelpOutletDirective();
    expect(directive).toBeTruthy();
  });
});
